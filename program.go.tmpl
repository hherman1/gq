package main
import "os"
{{.Prelude}}

func main() {
	if err := run(); err != nil {
		fmt.Println("Error: ", err)
		os.Exit(1)
	}
}

func run() error {
	j := new(Node)
	err := json.NewDecoder(os.Stdin).Decode(j)
	if err != nil {
		return fmt.Errorf("unmarshal: %w", err)
	}

	{{.Program}}

	fmt.Println(j)
	return nil
}